<h2>Cats for rent:</h2>

<ul>
    <% @cats.each do |cat| %>
        <li><a href="<%= cat_url(cat) %>"><%= cat.name %></a></li>
    <% end %>

</ul>

<a href="<%= new_cat_url %>">Create new cat!</a>  
